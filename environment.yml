name: deep-image-prior
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - jupyter
  - nbclient
  - nbformat
  - numpy
  - pyyaml
  - setuptools
  - cmake
  - cffi
  - matplotlib
  - scikit-image
  - pillow
  - opencv
  - pip
  - pip:
      - torch
      - torchvision
